package com.example.comida.ui.theme

import androidx.compose.ui.graphics.Color

// Colores extraídos de tu diseño MealCraft
val MealCream = Color(0xFFFFF8F0)      // Fondo principal
val MealBrown = Color(0xFF8D5A34)      // Color primario (botones, logos)
val MealDarkBrown = Color(0xFF5D3A20)  // Textos fuertes
val MealLightBeige = Color(0xFFF4DDBE) // Acentos suaves / botones secundarios
val MealGreyText = Color(0xFFA09080)   // Texto secundario (hints)
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)